<div class="comments">
    {{#each this}}
    <div class="comment">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </div>
    {{/each}}
</div>